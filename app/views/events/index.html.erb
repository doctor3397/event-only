<!-- t.string   "name"
t.integer  "location_id"
t.date     "date"
t.integer  "capacity" -->

<%= link_to "Back to #{@location.name}", location_path(id: @location.id) %>

<% @events.each do |event| %>
<%= render 'event', event: event %>
  <!-- <div>
    <h3>Name: <%= event.name %></h3>
    <p>Location ID: <%= event.location_id %></p>
    <p>Date: <%= event.date %></p>
    <p>Event ID: <%= event.id %></p>
  </div> -->
<%= link_to event.name, location_event_path(location_id: @location.id, id: event.id ) %>
<% end %>
